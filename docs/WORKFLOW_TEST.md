# 🧪 ワークフローテスト手順

## テスト準備完了

### ✅ 実装内容
1. **シンプル化されたエージェント**
   - `planner.md` - 1つのコンテキストファイルのみ使用
   - `executor.md` - シンプルな実装エージェント
   - `reviewer.md` - シンプルなレビューエージェント

2. **統合コンテキストファイル**
   - `docs/context.md` - すべての情報を1箇所に集約

3. **メインエージェント制御**
   - workflow_orchestratorを削除
   - メインエージェント（私）が直接制御

## 🚀 テスト方法

### 方法1: 手動ワークフロー
```bash
# 1. 計画作成
@planner "シンプルなHello Worldアプリを作成"

# 2. context.md確認後、実装
@executor "docs/context.mdのStep 1を実装"

# 3. レビュー
@reviewer "直前の実装をレビュー"
```

### 方法2: メインエージェント管理
私が直接以下を実行：
1. タスクを受け取る
2. @plannerを呼び出して計画作成
3. @executorを呼び出して実装
4. @reviewerを呼び出してレビュー
5. 必要に応じて繰り返し

## 📊 期待される動作

1. **可視性**: 各エージェント呼び出しがターミナルに表示
2. **シンプル**: docs/context.md 1ファイルのみ更新
3. **透明性**: 何が起きているか明確

## 🎯 テストシナリオ

### 小さなテスト
「Hello Worldを表示するindex.htmlを作成」

期待されるフロー：
1. planner: 1-2ステップの計画作成
2. executor: index.html作成
3. reviewer: レビューと確認

### 実際のテスト
「動画→MP3変換アプリ作成」のような実際のタスクで確認